# entities

Domain objects: `issue`, `project`, `user`, `organization`, etc. Each entity
owns its model, its API calls, and a presentational card/summary component.
Entities may import from `shared` only.

First entity lands in Phase 1.
