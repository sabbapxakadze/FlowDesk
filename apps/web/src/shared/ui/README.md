# shared/ui

The design system: Button, Dialog, Select, Card, etc. Knows nothing about the
domain (no "Issue", no "Project" in here). Consumes semantic design tokens
only, once they exist (Phase 3.5) — see docs/adr/0006.

Populated starting Phase 1, as real screens need real components.
