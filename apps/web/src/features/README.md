# features

A single user action: `create-issue`, `move-issue`, `assign-user`. Features
may import from `entities` and `shared`, not from `widgets`, `pages` or `app`.

First feature lands in Phase 1.
