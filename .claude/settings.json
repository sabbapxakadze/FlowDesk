{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "statusMessage": "Typechecking and linting…",
            "timeout": 120,
            "command": "node -e \"let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{try{const j=JSON.parse(d);process.stdout.write((j.tool_input&&j.tool_input.file_path)||'')}catch(e){}})\" | { read -r f; case \"$f\" in apps/*|'apps\\'*|*/apps/*|*'\\'apps'\\'*|packages/*|'packages\\'*|*/packages/*|*'\\'packages'\\'*) pnpm typecheck && pnpm lint ;; *) exit 0 ;; esac; }"
          }
        ]
      }
    ]
  }
}
